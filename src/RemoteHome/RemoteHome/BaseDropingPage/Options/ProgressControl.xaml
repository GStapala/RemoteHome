<?xml version="1.0" encoding="utf-8"?>

<ContentView xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="RemoteHome.BaseDropingPage.Options.ProgressControl">
    <Grid BackgroundColor="{Binding BackgroundColor}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <Label Grid.Column="0" VerticalTextAlignment="Center" HorizontalTextAlignment="Start" Text="{Binding Text}"
               Style="{StaticResource LabelGlobalSyle}" TextColor="{Binding TextColor}" />
        <Label Grid.Column="1" VerticalTextAlignment="Center" HorizontalTextAlignment="End" Text="{Binding Percentage}"
               Style="{StaticResource LabelGlobalSyle}" TextColor="{Binding TextColor}" />
        <ProgressBar Grid.Column="2" Progress="{Binding Progress}" />
    </Grid>
</ContentView>
using Xamarin.Forms;

namespace RemoteHomePrism.BaseDropingPage.Options
{
    /// <summary>
    ///     Simple switch control with text and formating
    /// </summary>
    public partial class SwitchControl : ContentView
    {
        public SwitchControl()
        {
            //var assembly = typeof(SwitchControl).GetTypeInfo().Assembly;
            //var ass = typeof(ImageResourceExtension).GetTypeInfo().Assembly;
            //foreach (var res in assembly.GetManifestResourceNames())
            //{
            //    System.Diagnostics.Debug.WriteLine("found resource: " + res);
            //}

            //foreach (var res in ass.GetManifestResourceNames())
            //{
            //    System.Diagnostics.Debug.WriteLine("Ass : found resource: " + res);
            //}


            InitializeComponent();

            // ...
            // NOTE: use for debugging, not in released app code!
        }
    }
}